# TripMate i18n — Multilingual Support Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add pt-BR / English / Spanish support to every user-visible string using react-i18next, with a language switcher on ProfilePage that persists to localStorage.

**Architecture:** `src/i18n/index.ts` initialises i18next with the browser-language detector and three locale JSON files. A global `react-i18next` mock is added to `src/test-setup.ts` so all existing component tests continue to pass. The `GapWarning.message` field is replaced with `fromTitle`/`toTitle` so pure utility functions stay free of i18n dependencies. Every component calls `useTranslation()` and replaces hardcoded strings with `t('key')`.

**Tech Stack:** React 19, TypeScript, i18next 23, react-i18next 14, i18next-browser-languagedetector 8, Vitest + @testing-library/react

**Spec:** `docs/superpowers/specs/2026-03-18-i18n-design.md`

---

## Chunk 1: Foundation — i18next setup + test mock + GapWarning refactor

### File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/test-setup.ts` | Modify | Add global react-i18next mock |
| `src/i18n/index.ts` | Create | i18next init with detector |
| `src/i18n/locales/en.json` | Create | English strings |
| `src/i18n/locales/pt-BR.json` | Create | Portuguese strings |
| `src/i18n/locales/es.json` | Create | Spanish strings |
| `src/main.tsx` | Modify | Import i18n init before render |
| `src/lib/gapDetection.ts` | Modify | Replace `message` with `fromTitle`/`toTitle` |
| `src/lib/gapDetection.test.ts` | Modify | Update assertions to new shape |
| `src/lib/useTimelineGroups.ts` | Modify | Update `RenderItem` gap variant |
| `src/lib/useTimelineGroups.test.ts` | Modify | Update gap literal objects |
| `src/components/GapWarningCard.tsx` | Modify | Accept `fromTitle`/`toTitle`, use `t()` |
| `src/pages/TripDetailPage.tsx` | Modify | Update GapWarningCard call site |
| `src/components/MapView.tsx` | Modify | Update GapWarningCard call site |
| `src/lib/buildMapSegments.test.ts` | Modify | Update 5 GapWarning literals from `message` to `fromTitle`/`toTitle` |

---

### Task 1: Install dependencies

- [ ] **Step 1: Install runtime packages**

```bash
pnpm add i18next react-i18next i18next-browser-languagedetector
```

Expected: All three appear in `dependencies` in `package.json`. No peer dep warnings.

- [ ] **Step 2: Verify build still works**

```bash
pnpm build 2>&1
```

Expected: No TypeScript errors.

---

### Task 2: Add global react-i18next mock to test setup

**Files:**
- Modify: `src/test-setup.ts`

- [ ] **Step 1: Update `src/test-setup.ts`**

Replace the entire file content:

```ts
import '@testing-library/jest-dom'
import { vi } from 'vitest'

vi.mock('react-i18next', () => ({
  useTranslation: () => ({
    t: (key: string, opts?: Record<string, unknown>) => {
      if (opts && typeof opts === 'object') {
        return Object.entries(opts).reduce(
          (s, [k, v]) => s.replace(new RegExp(`\\{\\{${k}\\}\\}`, 'g'), String(v)),
          key
        )
      }
      return key
    },
    i18n: { changeLanguage: vi.fn(), language: 'en' },
  }),
  initReactI18next: { type: '3rdParty', init: () => {} },
  Trans: ({ i18nKey }: { i18nKey: string }) => i18nKey,
}))
```

- [ ] **Step 2: Run all tests — verify they all still pass (no regressions)**

```bash
pnpm exec vitest run 2>&1
```

Expected: All existing tests pass. (The mock is registered in the global setup but not yet exercised — no component imports `react-i18next` yet. The mock is fully validated later in Task 4 Step 10 after `GapWarningCard` uses `useTranslation`.)

---

### Task 3: Create i18n config and locale files

**Files:**
- Create: `src/i18n/index.ts`
- Create: `src/i18n/locales/en.json`
- Create: `src/i18n/locales/pt-BR.json`
- Create: `src/i18n/locales/es.json`

- [ ] **Step 1: Create `src/i18n/index.ts`**

```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'
import en from './locales/en.json'
import ptBR from './locales/pt-BR.json'
import es from './locales/es.json'

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en:     { translation: en },
      'pt-BR': { translation: ptBR },
      es:     { translation: es },
    },
    fallbackLng: 'pt-BR',
    supportedLngs: ['en', 'pt-BR', 'es'],
    detection: {
      order: ['localStorage', 'navigator'],
      caches: ['localStorage'],
      lookupLocalStorage: 'i18nextLng',
    },
    interpolation: { escapeValue: false },
  })

export default i18n
```

- [ ] **Step 2: Create `src/i18n/locales/en.json`**

```json
{
  "auth": {
    "tagline": "Plan your trip. Your way.",
    "subtitle": "No agencies. No hassle. Just you and the world.",
    "welcomeBack": "Welcome back",
    "signInSubtitle": "Sign in to continue planning your adventures",
    "continueWithGoogle": "Continue with Google",
    "continueWithApple": "Continue with Apple",
    "orContinueWithEmail": "or continue with email",
    "emailAddress": "Email address",
    "emailPlaceholder": "you@example.com",
    "emailRequired": "Email is required",
    "emailInvalid": "Enter a valid email address",
    "password": "Password",
    "passwordRequired": "Password is required",
    "forgotPassword": "Forgot password?",
    "invalidCredentials": "Invalid email or password",
    "signIn": "Sign In",
    "noAccount": "Don't have an account?",
    "createOne": "Create one",
    "createAccount": "Create account",
    "createAccountSubtitle": "Join TripMate and start planning your adventures",
    "fullName": "Full name",
    "fullNameRequired": "Full name is required",
    "fullNamePlaceholder": "Your full name",
    "passwordMinPlaceholder": "Min. 6 characters",
    "confirmPassword": "Confirm password",
    "passwordMismatch": "Passwords do not match",
    "confirmPasswordPlaceholder": "Repeat your password",
    "passwordTooShort": "Password must be at least 6 characters",
    "createAccountButton": "Create Account",
    "checkEmailConfirm": "Check your email to confirm your account.",
    "onceConfirmedSignIn": "Once confirmed, sign in here.",
    "alreadyHaveAccount": "Already have an account?",
    "signInLink": "Sign in"
  },
  "nav": {
    "trips": "Trips",
    "profile": "Profile"
  },
  "trips": {
    "myTrips": "My Trips",
    "destinationCount_one": "{{count}} destination",
    "destinationCount_other": "{{count}} destinations",
    "newButton": "+ New",
    "emptyHeading": "No trips yet",
    "emptySubtext": "Start planning your next adventure",
    "planFirstTrip": "Plan your first trip",
    "planNewDestination": "+ Plan a new destination",
    "deleteConfirm": "Delete this destination and all its events?"
  },
  "trip": {
    "notFound": "Destination not found",
    "backToTrips": "← Back to trips",
    "tripsLink": "Trips",
    "addEvent": "Add event",
    "eventCount_one": "{{count}} event",
    "eventCount_other": "{{count}} events",
    "noEvents": "No events yet. Tap + to add your first.",
    "gapWarning": "No transport between \"{{from}}\" check-in and \"{{to}}\" check-in"
  },
  "eventTypes": {
    "transport": "Transport",
    "accommodation": "Stay",
    "ticket": "Ticket",
    "restaurant": "Food"
  },
  "destination": {
    "newTitle": "New Destination",
    "autoIcon": "Auto-assigned icon",
    "titlePlaceholder": "e.g. Japan 2026",
    "titleLabel": "Trip title",
    "titleRequired": "Title is required",
    "startDate": "Start Date",
    "endDate": "End Date",
    "endDateError": "End date must be after start date",
    "create": "Create Destination"
  },
  "event": {
    "addTitle": "Add Event",
    "editTitle": "Edit Event",
    "placeholderTransport": "e.g. Flight GRU → NRT",
    "placeholderAccommodation": "Hotel name",
    "placeholderTicket": "Museum or experience",
    "placeholderRestaurant": "Restaurant name",
    "titleLabel": "Event title",
    "titleRequired": "Title is required",
    "placeholderFrom": "🛫 From: departure place",
    "placeholderTo": "🛬 To: arrival place",
    "placeholderPlace": "📍 Search place",
    "placeRequired": "Place is required",
    "dateLabel": "Event date",
    "dateRequired": "Date is required",
    "timeLabel": "Event time",
    "departureTimeLabel": "Departure time",
    "arrivalTimePlaceholder": "Arrival time (optional)",
    "timeRequired": "Time is required",
    "arrivedOnFoot": "Arrived on foot",
    "costPlaceholder": "Cost (optional)",
    "costLabel": "Cost",
    "costError": "Must be a positive number",
    "notesPlaceholder": "Any notes...",
    "notesLabel": "Notes",
    "addButton": "Add to Timeline",
    "saveButton": "Save Changes",
    "deleteButton": "Delete event",
    "deleteConfirmLabel": "Are you sure you want to delete this event?",
    "confirmDeleteButton": "Confirm delete",
    "cancelButton": "Cancel",
    "editLabel": "Edit {{title}}"
  },
  "profile": {
    "onPlan": "You're on the {{plan}} plan",
    "plansButton": "Plans",
    "darkMode": "Dark mode",
    "logOut": "Log out"
  },
  "plans": {
    "close": "Close",
    "heading": "Choose your plan",
    "subtitle": "Upgrade anytime. Cancel anytime.",
    "monthly": "Monthly",
    "annual": "Annual",
    "savingsBadge": "−30%",
    "mostPopular": "MOST POPULAR",
    "alwaysFree": "always free",
    "perYear": "/year",
    "perMonth": "/month",
    "annualEquivPremium": "= R$13.25/mo · save R$69",
    "annualEquivPro": "= R$27.40/mo · save R$139",
    "free1trip": "✓ 1 trip",
    "free10events": "✓ Up to 10 events per trip",
    "premium10trips": "✓ Up to 10 trips",
    "premium30events": "✓ Up to 30 events per trip",
    "shareTrips": "✓ Share trips",
    "pro30trips": "✓ Up to 30 trips",
    "proUnlimited": "✓ Unlimited events",
    "currentPlan": "Current plan",
    "getPremium": "Get Premium",
    "getPro": "Get Pro",
    "footer": "Secure payment via Stripe · Cancel anytime"
  },
  "status": {
    "gap": "⚠️ GAP",
    "ok": "✓ OK"
  },
  "localImport": {
    "banner_one": "You have {{count}} trip saved locally",
    "banner_other": "You have {{count}} trips saved locally",
    "subtitle": "Import them to your account so they're backed up and available everywhere.",
    "importing": "Importing…",
    "importButton": "Import trips",
    "dismiss": "Dismiss"
  },
  "lang": {
    "label": "Language"
  }
}
```

- [ ] **Step 3: Create `src/i18n/locales/pt-BR.json`**

```json
{
  "auth": {
    "tagline": "Planeje sua viagem. Do seu jeito.",
    "subtitle": "Sem agências. Sem complicação. Só você e o mundo.",
    "welcomeBack": "Bem-vindo de volta",
    "signInSubtitle": "Entre para continuar planejando suas aventuras",
    "continueWithGoogle": "Continuar com Google",
    "continueWithApple": "Continuar com Apple",
    "orContinueWithEmail": "ou continue com email",
    "emailAddress": "Endereço de email",
    "emailPlaceholder": "voce@exemplo.com",
    "emailRequired": "Email é obrigatório",
    "emailInvalid": "Insira um endereço de email válido",
    "password": "Senha",
    "passwordRequired": "Senha é obrigatória",
    "forgotPassword": "Esqueceu a senha?",
    "invalidCredentials": "Email ou senha inválidos",
    "signIn": "Entrar",
    "noAccount": "Não tem uma conta?",
    "createOne": "Criar uma",
    "createAccount": "Criar conta",
    "createAccountSubtitle": "Entre no TripMate e comece a planejar suas aventuras",
    "fullName": "Nome completo",
    "fullNameRequired": "Nome completo é obrigatório",
    "fullNamePlaceholder": "Seu nome completo",
    "passwordMinPlaceholder": "Mín. 6 caracteres",
    "confirmPassword": "Confirmar senha",
    "passwordMismatch": "As senhas não coincidem",
    "confirmPasswordPlaceholder": "Repita sua senha",
    "passwordTooShort": "A senha deve ter pelo menos 6 caracteres",
    "createAccountButton": "Criar Conta",
    "checkEmailConfirm": "Verifique seu email para confirmar sua conta.",
    "onceConfirmedSignIn": "Após confirmar, entre aqui.",
    "alreadyHaveAccount": "Já tem uma conta?",
    "signInLink": "Entrar"
  },
  "nav": {
    "trips": "Viagens",
    "profile": "Perfil"
  },
  "trips": {
    "myTrips": "Minhas Viagens",
    "destinationCount_one": "{{count}} destino",
    "destinationCount_other": "{{count}} destinos",
    "newButton": "+ Novo",
    "emptyHeading": "Nenhuma viagem ainda",
    "emptySubtext": "Comece a planejar sua próxima aventura",
    "planFirstTrip": "Planejar minha primeira viagem",
    "planNewDestination": "+ Planejar novo destino",
    "deleteConfirm": "Excluir este destino e todos os seus eventos?"
  },
  "trip": {
    "notFound": "Destino não encontrado",
    "backToTrips": "← Voltar para viagens",
    "tripsLink": "Viagens",
    "addEvent": "Adicionar evento",
    "eventCount_one": "{{count}} evento",
    "eventCount_other": "{{count}} eventos",
    "noEvents": "Nenhum evento ainda. Toque + para adicionar.",
    "gapWarning": "Sem transporte entre o check-in em \"{{from}}\" e check-in em \"{{to}}\""
  },
  "eventTypes": {
    "transport": "Transporte",
    "accommodation": "Hospedagem",
    "ticket": "Ingresso",
    "restaurant": "Alimentação"
  },
  "destination": {
    "newTitle": "Novo Destino",
    "autoIcon": "Ícone atribuído automaticamente",
    "titlePlaceholder": "ex: Japão 2026",
    "titleLabel": "Título da viagem",
    "titleRequired": "Título é obrigatório",
    "startDate": "Data de Início",
    "endDate": "Data de Fim",
    "endDateError": "Data de fim deve ser após a data de início",
    "create": "Criar Destino"
  },
  "event": {
    "addTitle": "Adicionar Evento",
    "editTitle": "Editar Evento",
    "placeholderTransport": "ex: Voo GRU → NRT",
    "placeholderAccommodation": "Nome do hotel",
    "placeholderTicket": "Museu ou experiência",
    "placeholderRestaurant": "Nome do restaurante",
    "titleLabel": "Título do evento",
    "titleRequired": "Título é obrigatório",
    "placeholderFrom": "🛫 De: local de partida",
    "placeholderTo": "🛬 Para: local de chegada",
    "placeholderPlace": "📍 Buscar local",
    "placeRequired": "Local é obrigatório",
    "dateLabel": "Data do evento",
    "dateRequired": "Data é obrigatória",
    "timeLabel": "Hora do evento",
    "departureTimeLabel": "Hora de partida",
    "arrivalTimePlaceholder": "Hora de chegada (opcional)",
    "timeRequired": "Hora é obrigatória",
    "arrivedOnFoot": "Chegou a pé",
    "costPlaceholder": "Custo (opcional)",
    "costLabel": "Custo",
    "costError": "Deve ser um número positivo",
    "notesPlaceholder": "Alguma observação...",
    "notesLabel": "Notas",
    "addButton": "Adicionar à Linha do Tempo",
    "saveButton": "Salvar Alterações",
    "deleteButton": "Excluir evento",
    "deleteConfirmLabel": "Tem certeza que deseja excluir este evento?",
    "confirmDeleteButton": "Confirmar exclusão",
    "cancelButton": "Cancelar",
    "editLabel": "Editar {{title}}"
  },
  "profile": {
    "onPlan": "Você está no plano {{plan}}",
    "plansButton": "Planos",
    "darkMode": "Modo escuro",
    "logOut": "Sair"
  },
  "plans": {
    "close": "Fechar",
    "heading": "Escolha seu plano",
    "subtitle": "Atualize quando quiser. Cancele quando quiser.",
    "monthly": "Mensal",
    "annual": "Anual",
    "savingsBadge": "−30%",
    "mostPopular": "MAIS POPULAR",
    "alwaysFree": "sempre grátis",
    "perYear": "/ano",
    "perMonth": "/mês",
    "annualEquivPremium": "= R$13,25/mês · economize R$69",
    "annualEquivPro": "= R$27,40/mês · economize R$139",
    "free1trip": "✓ 1 viagem",
    "free10events": "✓ Até 10 eventos por viagem",
    "premium10trips": "✓ Até 10 viagens",
    "premium30events": "✓ Até 30 eventos por viagem",
    "shareTrips": "✓ Compartilhar viagens",
    "pro30trips": "✓ Até 30 viagens",
    "proUnlimited": "✓ Eventos ilimitados",
    "currentPlan": "Plano atual",
    "getPremium": "Obter Premium",
    "getPro": "Obter Pro",
    "footer": "Pagamento seguro via Stripe · Cancele quando quiser"
  },
  "status": {
    "gap": "⚠️ GAP",
    "ok": "✓ OK"
  },
  "localImport": {
    "banner_one": "Você tem {{count}} viagem salva localmente",
    "banner_other": "Você tem {{count}} viagens salvas localmente",
    "subtitle": "Importe-as para sua conta para que fiquem salvas e disponíveis em todos os lugares.",
    "importing": "Importando…",
    "importButton": "Importar viagens",
    "dismiss": "Dispensar"
  },
  "lang": {
    "label": "Idioma"
  }
}
```

- [ ] **Step 4: Create `src/i18n/locales/es.json`**

```json
{
  "auth": {
    "tagline": "Planifica tu viaje. A tu manera.",
    "subtitle": "Sin agencias. Sin complicaciones. Solo tú y el mundo.",
    "welcomeBack": "Bienvenido de vuelta",
    "signInSubtitle": "Inicia sesión para continuar planeando tus aventuras",
    "continueWithGoogle": "Continuar con Google",
    "continueWithApple": "Continuar con Apple",
    "orContinueWithEmail": "o continúa con email",
    "emailAddress": "Dirección de email",
    "emailPlaceholder": "tu@ejemplo.com",
    "emailRequired": "El email es obligatorio",
    "emailInvalid": "Ingresa un email válido",
    "password": "Contraseña",
    "passwordRequired": "La contraseña es obligatoria",
    "forgotPassword": "¿Olvidaste tu contraseña?",
    "invalidCredentials": "Email o contraseña incorrectos",
    "signIn": "Iniciar sesión",
    "noAccount": "¿No tienes una cuenta?",
    "createOne": "Crear una",
    "createAccount": "Crear cuenta",
    "createAccountSubtitle": "Únete a TripMate y empieza a planificar tus aventuras",
    "fullName": "Nombre completo",
    "fullNameRequired": "El nombre completo es obligatorio",
    "fullNamePlaceholder": "Tu nombre completo",
    "passwordMinPlaceholder": "Mín. 6 caracteres",
    "confirmPassword": "Confirmar contraseña",
    "passwordMismatch": "Las contraseñas no coinciden",
    "confirmPasswordPlaceholder": "Repite tu contraseña",
    "passwordTooShort": "La contraseña debe tener al menos 6 caracteres",
    "createAccountButton": "Crear Cuenta",
    "checkEmailConfirm": "Revisa tu email para confirmar tu cuenta.",
    "onceConfirmedSignIn": "Una vez confirmado, inicia sesión aquí.",
    "alreadyHaveAccount": "¿Ya tienes una cuenta?",
    "signInLink": "Iniciar sesión"
  },
  "nav": {
    "trips": "Viajes",
    "profile": "Perfil"
  },
  "trips": {
    "myTrips": "Mis Viajes",
    "destinationCount_one": "{{count}} destino",
    "destinationCount_other": "{{count}} destinos",
    "newButton": "+ Nuevo",
    "emptyHeading": "Sin viajes aún",
    "emptySubtext": "Empieza a planificar tu próxima aventura",
    "planFirstTrip": "Planifica tu primer viaje",
    "planNewDestination": "+ Planificar nuevo destino",
    "deleteConfirm": "¿Eliminar este destino y todos sus eventos?"
  },
  "trip": {
    "notFound": "Destino no encontrado",
    "backToTrips": "← Volver a viajes",
    "tripsLink": "Viajes",
    "addEvent": "Agregar evento",
    "eventCount_one": "{{count}} evento",
    "eventCount_other": "{{count}} eventos",
    "noEvents": "Sin eventos aún. Toca + para agregar.",
    "gapWarning": "Sin transporte entre el check-in en \"{{from}}\" y check-in en \"{{to}}\""
  },
  "eventTypes": {
    "transport": "Transporte",
    "accommodation": "Alojamiento",
    "ticket": "Entrada",
    "restaurant": "Comida"
  },
  "destination": {
    "newTitle": "Nuevo Destino",
    "autoIcon": "Ícono asignado automáticamente",
    "titlePlaceholder": "ej: Japón 2026",
    "titleLabel": "Título del viaje",
    "titleRequired": "El título es obligatorio",
    "startDate": "Fecha de inicio",
    "endDate": "Fecha de fin",
    "endDateError": "La fecha de fin debe ser posterior a la de inicio",
    "create": "Crear Destino"
  },
  "event": {
    "addTitle": "Agregar Evento",
    "editTitle": "Editar Evento",
    "placeholderTransport": "ej: Vuelo GRU → NRT",
    "placeholderAccommodation": "Nombre del hotel",
    "placeholderTicket": "Museo o experiencia",
    "placeholderRestaurant": "Nombre del restaurante",
    "titleLabel": "Título del evento",
    "titleRequired": "El título es obligatorio",
    "placeholderFrom": "🛫 De: lugar de salida",
    "placeholderTo": "🛬 A: lugar de llegada",
    "placeholderPlace": "📍 Buscar lugar",
    "placeRequired": "El lugar es obligatorio",
    "dateLabel": "Fecha del evento",
    "dateRequired": "La fecha es obligatoria",
    "timeLabel": "Hora del evento",
    "departureTimeLabel": "Hora de salida",
    "arrivalTimePlaceholder": "Hora de llegada (opcional)",
    "timeRequired": "La hora es obligatoria",
    "arrivedOnFoot": "Llegó a pie",
    "costPlaceholder": "Costo (opcional)",
    "costLabel": "Costo",
    "costError": "Debe ser un número positivo",
    "notesPlaceholder": "Alguna nota...",
    "notesLabel": "Notas",
    "addButton": "Agregar al Itinerario",
    "saveButton": "Guardar Cambios",
    "deleteButton": "Eliminar evento",
    "deleteConfirmLabel": "¿Estás seguro de que quieres eliminar este evento?",
    "confirmDeleteButton": "Confirmar eliminación",
    "cancelButton": "Cancelar",
    "editLabel": "Editar {{title}}"
  },
  "profile": {
    "onPlan": "Estás en el plan {{plan}}",
    "plansButton": "Planes",
    "darkMode": "Modo oscuro",
    "logOut": "Cerrar sesión"
  },
  "plans": {
    "close": "Cerrar",
    "heading": "Elige tu plan",
    "subtitle": "Actualiza cuando quieras. Cancela cuando quieras.",
    "monthly": "Mensual",
    "annual": "Anual",
    "savingsBadge": "−30%",
    "mostPopular": "MÁS POPULAR",
    "alwaysFree": "siempre gratis",
    "perYear": "/año",
    "perMonth": "/mes",
    "annualEquivPremium": "= R$13,25/mes · ahorra R$69",
    "annualEquivPro": "= R$27,40/mes · ahorra R$139",
    "free1trip": "✓ 1 viaje",
    "free10events": "✓ Hasta 10 eventos por viaje",
    "premium10trips": "✓ Hasta 10 viajes",
    "premium30events": "✓ Hasta 30 eventos por viaje",
    "shareTrips": "✓ Compartir viajes",
    "pro30trips": "✓ Hasta 30 viajes",
    "proUnlimited": "✓ Eventos ilimitados",
    "currentPlan": "Plan actual",
    "getPremium": "Obtener Premium",
    "getPro": "Obtener Pro",
    "footer": "Pago seguro via Stripe · Cancela cuando quieras"
  },
  "status": {
    "gap": "⚠️ GAP",
    "ok": "✓ OK"
  },
  "localImport": {
    "banner_one": "Tienes {{count}} viaje guardado localmente",
    "banner_other": "Tienes {{count}} viajes guardados localmente",
    "subtitle": "Impórtalos a tu cuenta para tenerlos respaldados y disponibles en todas partes.",
    "importing": "Importando…",
    "importButton": "Importar viajes",
    "dismiss": "Descartar"
  },
  "lang": {
    "label": "Idioma"
  }
}
```

- [ ] **Step 5: Wire i18n into `src/main.tsx`**

Add the import as the very first import in the file:

```tsx
import './i18n'
import '@fontsource/inter/400.css'
import '@fontsource/inter/600.css'
import '@fontsource/inter/700.css'
import '@fontsource/inter/800.css'
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 6: Verify TypeScript build**

```bash
pnpm build 2>&1
```

Expected: No TypeScript errors.

- [ ] **Step 7: Run all tests**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests still pass.

- [ ] **Step 8: Commit**

```bash
git add src/test-setup.ts src/i18n/ src/main.tsx
git commit -m "feat: add i18next config, locale files, and test mock"
```

---

### Task 4: Refactor GapWarning — replace `message` with `fromTitle`/`toTitle`

**Files:**
- Modify: `src/lib/gapDetection.ts`
- Modify: `src/lib/gapDetection.test.ts`
- Modify: `src/lib/useTimelineGroups.ts`
- Modify: `src/lib/useTimelineGroups.test.ts`
- Modify: `src/components/GapWarningCard.tsx`
- Modify: `src/pages/TripDetailPage.tsx`
- Modify: `src/components/MapView.tsx`

- [ ] **Step 1: Update `src/lib/gapDetection.ts`**

Change the `GapWarning` interface and the `gaps.push()` call:

```ts
import type { TripEvent } from '../types/trip'

export interface GapWarning {
  afterEventId: string
  beforeEventId: string
  fromTitle: string  // title of the first accommodation
  toTitle: string    // title of the second accommodation
}

export function detectGaps(events: TripEvent[]): GapWarning[] {
  const sorted = [...events].sort((a, b) =>
    `${a.date} ${a.time}`.localeCompare(`${b.date} ${b.time}`)
  )

  const accommodations = sorted.filter(e => e.type === 'accommodation')
  const gaps: GapWarning[] = []

  for (let i = 0; i < accommodations.length - 1; i++) {
    const a = accommodations[i]
    const b = accommodations[i + 1]

    const hasWalkingRoute = b.arrivedOnFoot === true

    const hasTransportToB = sorted.some(
      e =>
        e.type === 'transport' &&
        e.date >= a.date &&
        e.date <= b.date &&
        e.placeIdTo !== undefined &&
        e.placeIdTo === b.placeId,
    )

    if (!hasWalkingRoute && !hasTransportToB) {
      gaps.push({
        afterEventId: a.id,
        beforeEventId: b.id,
        fromTitle: a.title,
        toTitle: b.title,
      })
    }
  }

  return gaps
}
```

- [ ] **Step 2: Update `src/lib/gapDetection.test.ts`**

Replace the two assertions that check `gap.message` with assertions on `fromTitle`/`toTitle`:

```ts
  it('returns a gap when destination accommodation has no arrivedOnFoot flag', () => {
    const events = [
      makeEvent({ id: 'acc-1', type: 'accommodation', title: 'Hotel A', date: '2026-03-15', time: '14:00' }),
      makeEvent({ id: 'acc-2', type: 'accommodation', title: 'Hotel B', date: '2026-03-18', time: '15:00' }),
    ]
    const gaps = detectGaps(events)
    expect(gaps).toHaveLength(1)
    expect(gaps[0].afterEventId).toBe('acc-1')
    expect(gaps[0].beforeEventId).toBe('acc-2')
    expect(gaps[0].fromTitle).toBe('Hotel A')
    expect(gaps[0].toTitle).toBe('Hotel B')
  })
```

- [ ] **Step 3: Run gapDetection tests — verify all pass**

```bash
pnpm exec vitest run src/lib/gapDetection.test.ts 2>&1
```

Expected: All tests pass.

- [ ] **Step 4: Update `src/lib/useTimelineGroups.ts`**

Replace `message: string` with `fromTitle: string; toTitle: string` in `RenderItem`, and update the push call:

```ts
import type { TripEvent } from '../types/trip'
import type { GapWarning } from './gapDetection'

export type RenderItem =
  | { kind: 'event'; event: TripEvent }
  | { kind: 'gap'; fromTitle: string; toTitle: string; key: string }

export interface TimelineGroup {
  date: string
  label: string
  items: RenderItem[]
}

const WEEKDAYS = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']
const MONTHS = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']

function formatGroupLabel(date: string): string {
  const [y, m, d] = date.split('-').map(Number)
  const dt = new Date(y, m - 1, d)
  return `${WEEKDAYS[dt.getDay()]}, ${d} ${MONTHS[m - 1]} ${y}`
}

export function useTimelineGroups(
  sortedEvents: TripEvent[],
  gaps: GapWarning[]
): TimelineGroup[] {
  const groups: TimelineGroup[] = []
  const groupByDate = new Map<string, TimelineGroup>()

  for (const event of sortedEvents) {
    if (!groupByDate.has(event.date)) {
      const group: TimelineGroup = {
        date: event.date,
        label: formatGroupLabel(event.date),
        items: [],
      }
      groupByDate.set(event.date, group)
      groups.push(group)
    }
    const group = groupByDate.get(event.date)!
    group.items.push({ kind: 'event', event })
    const gap = gaps.find(g => g.afterEventId === event.id)
    if (gap) {
      group.items.push({
        kind: 'gap',
        fromTitle: gap.fromTitle,
        toTitle: gap.toTitle,
        key: `gap-${gap.afterEventId}`,
      })
    }
  }

  return groups
}
```

- [ ] **Step 5: Update `src/lib/useTimelineGroups.test.ts`**

Replace all `{ message: '...' }` gap literals with `{ fromTitle: '...', toTitle: '...' }`. Find each line by its string content and replace it:

Find the literal containing `message: 'Gap message'`:
```ts
    const gaps: GapWarning[] = [{ afterEventId: 'a', beforeEventId: 'c', message: 'Gap message' }]
```
Replace with:
```ts
    const gaps: GapWarning[] = [{ afterEventId: 'a', beforeEventId: 'c', fromTitle: 'Hotel A', toTitle: 'Hotel C' }]
```

Find the literal containing `message: 'No transport'`:
```ts
    const gaps: GapWarning[] = [{ afterEventId: 'a', beforeEventId: 'b', message: 'No transport' }]
```
Replace with:
```ts
    const gaps: GapWarning[] = [{ afterEventId: 'a', beforeEventId: 'b', fromTitle: 'Hotel A', toTitle: 'Hotel B' }]
```

- [ ] **Step 6: Update `src/lib/buildMapSegments.test.ts`**

This file constructs `GapWarning` literals with `message: 'No transport'`. Replace all five with `fromTitle`/`toTitle`. Find each one by content and replace:

Find:
```ts
      { afterEventId: 'acc-1', beforeEventId: 'acc-2', message: 'No transport' },
```
Replace with (use this for ALL occurrences of this pattern — there are 3):
```ts
      { afterEventId: 'acc-1', beforeEventId: 'acc-2', fromTitle: 'Hotel A', toTitle: 'Hotel B' },
```

Find:
```ts
      { afterEventId: 'acc-1', beforeEventId: 'acc-3', message: 'No transport' },
```
Replace with:
```ts
      { afterEventId: 'acc-1', beforeEventId: 'acc-3', fromTitle: 'Hotel A', toTitle: 'Hotel C' },
```

Find:
```ts
      { afterEventId: 'b', beforeEventId: 'c', message: 'No transport' },
```
Replace with:
```ts
      { afterEventId: 'b', beforeEventId: 'c', fromTitle: 'Hotel B', toTitle: 'Hotel C' },
```

Find:
```ts
    const gaps: GapWarning[] = [{ afterEventId: 'a1', beforeEventId: 'a2', message: 'No transport' }]
```
Replace with:
```ts
    const gaps: GapWarning[] = [{ afterEventId: 'a1', beforeEventId: 'a2', fromTitle: 'Hotel A', toTitle: 'Hotel B' }]
```

- [ ] **Step 7: Run useTimelineGroups and buildMapSegments tests — verify all pass**

```bash
pnpm exec vitest run src/lib/useTimelineGroups.test.ts src/lib/buildMapSegments.test.ts 2>&1
```

Expected: All tests pass.

- [ ] **Step 8: Update `src/components/GapWarningCard.tsx`**

Replace the entire file:

```tsx
import { useTranslation } from 'react-i18next'

interface Props {
  fromTitle: string
  toTitle: string
}

export default function GapWarningCard({ fromTitle, toTitle }: Props) {
  const { t } = useTranslation()
  return (
    <div
      className="border border-dashed border-primary rounded-lg px-3 py-2 bg-[#FFF7ED] dark:bg-transparent"
      role="alert"
    >
      <p className="text-xs text-[#C75B2A] font-medium">
        ⚠️ {t('trip.gapWarning', { from: fromTitle, to: toTitle })}
      </p>
    </div>
  )
}
```

- [ ] **Step 9: Update `src/pages/TripDetailPage.tsx`**

Find the JSX line with `message={item.message}` in TripDetailPage and change:

Change:
```tsx
<GapWarningCard key={item.key} message={item.message} />
```
to:
```tsx
<GapWarningCard key={item.key} fromTitle={item.fromTitle} toTitle={item.toTitle} />
```

- [ ] **Step 10: Update `src/components/MapView.tsx`**

Find the JSX line with `message={item.message}` in MapView and change:
```tsx
<GapWarningCard key={item.key} message={item.message} />
```
to:
```tsx
<GapWarningCard key={item.key} fromTitle={item.fromTitle} toTitle={item.toTitle} />
```

- [ ] **Step 11: Run all tests — verify no regressions**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass.

- [ ] **Step 12: TypeScript build check**

```bash
pnpm build 2>&1
```

Expected: No errors.

- [ ] **Step 13: Commit**

```bash
git add src/lib/gapDetection.ts src/lib/gapDetection.test.ts src/lib/useTimelineGroups.ts src/lib/useTimelineGroups.test.ts src/lib/buildMapSegments.test.ts src/components/GapWarningCard.tsx src/pages/TripDetailPage.tsx src/components/MapView.tsx
git commit -m "refactor: replace GapWarning.message with fromTitle/toTitle for i18n"
```

---

## Chunk 2: LanguageSwitcher + ProfilePage

### File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/components/LanguageSwitcher.tsx` | Create | 3-button PT/EN/ES switcher |
| `src/components/LanguageSwitcher.test.tsx` | Create | TDD tests |
| `src/pages/ProfilePage.tsx` | Modify | Add Language row with LanguageSwitcher, use `t()` |

---

### Task 5: LanguageSwitcher component (TDD)

- [ ] **Step 1: Write failing tests — create `src/components/LanguageSwitcher.test.tsx`**

```tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import LanguageSwitcher from './LanguageSwitcher'

// The global mock in test-setup.ts stubs useTranslation.
// We need to control i18n.language per test, so we override here.
const mockChangeLanguage = vi.fn()
let currentLanguage = 'en'

vi.mock('react-i18next', () => ({
  useTranslation: () => ({
    t: (key: string) => key,
    i18n: {
      changeLanguage: mockChangeLanguage,
      get language() { return currentLanguage },
    },
  }),
}))

beforeEach(() => {
  mockChangeLanguage.mockClear()
  currentLanguage = 'en'
})

describe('LanguageSwitcher', () => {
  it('renders PT, EN, ES buttons', () => {
    render(<LanguageSwitcher />)
    expect(screen.getByText('PT')).toBeInTheDocument()
    expect(screen.getByText('EN')).toBeInTheDocument()
    expect(screen.getByText('ES')).toBeInTheDocument()
  })

  it('clicking EN calls i18n.changeLanguage with "en"', () => {
    render(<LanguageSwitcher />)
    fireEvent.click(screen.getByText('EN'))
    expect(mockChangeLanguage).toHaveBeenCalledWith('en')
  })

  it('clicking PT calls i18n.changeLanguage with "pt-BR"', () => {
    render(<LanguageSwitcher />)
    fireEvent.click(screen.getByText('PT'))
    expect(mockChangeLanguage).toHaveBeenCalledWith('pt-BR')
  })

  it('clicking ES calls i18n.changeLanguage with "es"', () => {
    render(<LanguageSwitcher />)
    fireEvent.click(screen.getByText('ES'))
    expect(mockChangeLanguage).toHaveBeenCalledWith('es')
  })

  it('active language button has aria-pressed="true"', () => {
    currentLanguage = 'en'
    render(<LanguageSwitcher />)
    expect(screen.getByText('EN').closest('button')).toHaveAttribute('aria-pressed', 'true')
    expect(screen.getByText('PT').closest('button')).toHaveAttribute('aria-pressed', 'false')
    expect(screen.getByText('ES').closest('button')).toHaveAttribute('aria-pressed', 'false')
  })
})
```

- [ ] **Step 2: Run tests — verify they ALL fail**

```bash
pnpm exec vitest run src/components/LanguageSwitcher.test.tsx 2>&1
```

Expected: All 5 tests FAIL with "Cannot find module './LanguageSwitcher'".

- [ ] **Step 3: Create `src/components/LanguageSwitcher.tsx`**

```tsx
import { useTranslation } from 'react-i18next'
import { cn } from '@/lib/utils'

const LANGUAGES = [
  { code: 'pt-BR', label: 'PT' },
  { code: 'en',   label: 'EN' },
  { code: 'es',   label: 'ES' },
]

export default function LanguageSwitcher() {
  const { i18n } = useTranslation()
  return (
    <div className="flex gap-1" role="group" aria-label="Language">
      {LANGUAGES.map(({ code, label }) => (
        <button
          key={code}
          onClick={() => i18n.changeLanguage(code)}
          aria-pressed={i18n.language === code}
          className={cn(
            'px-2.5 py-1 rounded-lg text-xs font-semibold transition-colors',
            i18n.language === code
              ? 'bg-primary text-white'
              : 'text-muted hover:text-foreground'
          )}
        >
          {label}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 4: Run tests — verify all 5 pass**

```bash
pnpm exec vitest run src/components/LanguageSwitcher.test.tsx 2>&1
```

Expected: `5 passed`.

- [ ] **Step 5: Update `src/pages/ProfilePage.tsx`**

Add imports at the top:
```tsx
import { useTranslation } from 'react-i18next'
import LanguageSwitcher from '@/components/LanguageSwitcher'
```

Add `const { t } = useTranslation()` inside the component (after existing hooks).

Replace the plan badge constant:
```tsx
const PLAN_BADGE: Record<Plan, { label: string; className: string }> = {
  free: { label: 'Free', className: 'bg-stone-500' },
  premium: { label: 'Premium', className: 'bg-blue-500' },
  pro: { label: 'Pro ✦', className: 'bg-gradient-to-r from-amber-400 to-amber-600' },
}
```
Leave plan badge labels as hardcoded English (they are proper nouns / brand names shared across languages — not user-facing translated text).

Replace the "You're on the X plan" paragraph. The current source has `{badge.label}` wrapped in a `<span className="font-semibold">` — remove the span and surrounding text fragments entirely, replacing the whole paragraph content with a single `t()` call:
```tsx
<p className="text-xs text-muted mb-3">
  {t('profile.onPlan', { plan: badge.label })}
</p>
```

Replace the Plans button:
```tsx
<Button className="w-full" onClick={() => setShowPlans(true)}>
  {t('profile.plansButton')}
</Button>
```

Replace the Dark mode label:
```tsx
<span className="text-sm font-semibold text-foreground">{t('profile.darkMode')}</span>
```

Add Language row after the Dark mode row:
```tsx
<div className="bg-white dark:bg-transparent border border-border rounded-xl px-4 py-3 w-full flex justify-between items-center">
  <span className="text-sm font-semibold text-foreground">{t('lang.label')}</span>
  <LanguageSwitcher />
</div>
```

Replace the Log out button:
```tsx
<button
  onClick={handleLogout}
  className="w-full border border-red-400 text-red-500 rounded-xl py-3 text-sm font-semibold hover:bg-red-50 dark:hover:bg-red-950/20 transition-colors"
>
  {t('profile.logOut')}
</button>
```

- [ ] **Step 6: Run all tests**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass.

- [ ] **Step 7: Build check**

```bash
pnpm build 2>&1
```

Expected: No TypeScript errors.

- [ ] **Step 8: Commit**

```bash
git add src/components/LanguageSwitcher.tsx src/components/LanguageSwitcher.test.tsx src/pages/ProfilePage.tsx
git commit -m "feat: add LanguageSwitcher + wire ProfilePage i18n"
```

---

## Chunk 3: Auth pages + Nav + BottomNav

### File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/pages/LoginPage.tsx` | Modify | Use `t()` for all strings |
| `src/pages/SignupPage.tsx` | Modify | Use `t()` for all strings |
| `src/components/BottomNav.tsx` | Modify | Use `t()` for nav labels |

---

### Task 6: Translate LoginPage, SignupPage, BottomNav

- [ ] **Step 1: Read `src/pages/LoginPage.tsx` in full**

Read the file to understand exact current content before editing.

- [ ] **Step 2: Update `src/pages/LoginPage.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Replace every hardcoded string with the matching `t('auth.*')` key from the locale files. Key mappings:
- `"Plan your trip. Your way."` → `t('auth.tagline')`
- `"No agencies. No hassle. Just you and the world."` → `t('auth.subtitle')`
- `"Welcome back"` → `t('auth.welcomeBack')`
- `"Sign in to continue planning your adventures"` → `t('auth.signInSubtitle')`
- `"Continue with Google"` → `t('auth.continueWithGoogle')`
- `"Continue with Apple"` → `t('auth.continueWithApple')`
- `"or continue with email"` → `t('auth.orContinueWithEmail')`
- `"Email address"` → `t('auth.emailAddress')`
- placeholder `"you@example.com"` → `t('auth.emailPlaceholder')`
- `"Email is required"` → `t('auth.emailRequired')`
- `"Enter a valid email address"` → `t('auth.emailInvalid')`
- `"Password"` → `t('auth.password')`
- placeholder `"••••••••"` — leave as-is (it's a visual mask, not text)
- `"Password is required"` → `t('auth.passwordRequired')`
- `"Forgot password?"` → `t('auth.forgotPassword')`
- `"Invalid email or password"` → `t('auth.invalidCredentials')`
- `"Sign In"` button text and aria-label → `t('auth.signIn')`
- `"Don't have an account?"` → `t('auth.noAccount')`
- `"Create one"` → `t('auth.createOne')`

- [ ] **Step 3: Read `src/pages/SignupPage.tsx` in full**

Read the file to understand exact current content before editing.

- [ ] **Step 4: Update `src/pages/SignupPage.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

If SignupPage renders "Continue with Google" or "Continue with Apple" social auth buttons (check the file), replace them with `t('auth.continueWithGoogle')` and `t('auth.continueWithApple')`.

Replace every hardcoded string with the matching `t('auth.*')` key:
- `"Create account"` → `t('auth.createAccount')`
- `"Join TripMate and start planning your adventures"` → `t('auth.createAccountSubtitle')`
- `"Full name"` → `t('auth.fullName')`
- `"Full name is required"` → `t('auth.fullNameRequired')`
- placeholder `"Your full name"` → `t('auth.fullNamePlaceholder')`
- `"Email address"` → `t('auth.emailAddress')`
- placeholder `"you@example.com"` → `t('auth.emailPlaceholder')`
- `"Email is required"` → `t('auth.emailRequired')`
- `"Enter a valid email address"` → `t('auth.emailInvalid')`
- `"Password"` → `t('auth.password')`
- placeholder `"Min. 6 characters"` → `t('auth.passwordMinPlaceholder')`
- `"Password is required"` → `t('auth.passwordRequired')`
- `"Password must be at least 6 characters"` → `t('auth.passwordTooShort')`
- `"Confirm password"` → `t('auth.confirmPassword')`
- placeholder `"Repeat your password"` → `t('auth.confirmPasswordPlaceholder')`
- `"Passwords do not match"` → `t('auth.passwordMismatch')`
- `"Create Account"` button → `t('auth.createAccountButton')`
- success: `"Check your email to confirm your account."` → `t('auth.checkEmailConfirm')`
- success: `"Once confirmed, sign in here."` → `t('auth.onceConfirmedSignIn')`
- `"Already have an account?"` → `t('auth.alreadyHaveAccount')`
- `"Sign in"` link → `t('auth.signInLink')`

- [ ] **Step 5: Update `src/components/BottomNav.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Replace:
- `"Trips"` text → `{t('nav.trips')}`
- `"Profile"` text → `{t('nav.profile')}`

- [ ] **Step 6: Run all tests**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass. (LoginPage tests check keys now, not translated values — this is expected behaviour with the global mock.)

- [ ] **Step 7: Build check**

```bash
pnpm build 2>&1
```

Expected: No errors.

- [ ] **Step 8: Commit**

```bash
git add src/pages/LoginPage.tsx src/pages/SignupPage.tsx src/components/BottomNav.tsx
git commit -m "feat: translate LoginPage, SignupPage, BottomNav"
```

---

## Chunk 4: Trip pages and destination sheets

### File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/pages/TripsPage.tsx` | Modify | Use `t()` for all strings |
| `src/pages/TripDetailPage.tsx` | Modify | Use `t()` for remaining strings |
| `src/components/DestinationRow.tsx` | Modify | Use `t()` for confirm and badges |
| `src/components/sheets/NewDestinationSheet.tsx` | Modify | Use `t()` for all strings |

---

### Task 7: Translate TripsPage and related components

- [ ] **Step 1: Read the four files in full**

Read:
- `src/pages/TripsPage.tsx`
- `src/pages/TripDetailPage.tsx`
- `src/components/DestinationRow.tsx`
- `src/components/sheets/NewDestinationSheet.tsx`

- [ ] **Step 2: Update `src/pages/TripsPage.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Replace:
- `"My Trips"` → `{t('trips.myTrips')}`
- `{destinations.length} destination{...}` → `{t('trips.destinationCount', { count: destinations.length })}`
- `"+ New"` → `{t('trips.newButton')}`
- `"No trips yet"` → `{t('trips.emptyHeading')}`
- `"Start planning your next adventure"` → `{t('trips.emptySubtext')}`
- `"Plan your first trip"` → `{t('trips.planFirstTrip')}`
- `"+ Plan a new destination"` → `{t('trips.planNewDestination')}`

- [ ] **Step 3: Update `src/pages/TripDetailPage.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Replace:
- `"Destination not found"` → `{t('trip.notFound')}`
- `"← Back to trips"` → `{t('trip.backToTrips')}`
- The back link — find the text node `Trips` (the text after a `<ChevronLeft>` icon) and replace that text node with `{t('trip.tripsLink')}`
- `aria-label="Add event"` → `aria-label={t('trip.addEvent')}`
- `{destination.events.length} event{...}` → `{t('trip.eventCount', { count: destination.events.length })}`
- `"No events yet. Tap + to add your first."` → `{t('trip.noEvents')}`

- [ ] **Step 4: Update `src/components/DestinationRow.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Replace:
- `window.confirm('Delete this destination and all its events?')` → `window.confirm(t('trips.deleteConfirm'))`
- `"⚠️ GAP"` badge text → `{t('status.gap')}`
- `"✓ OK"` badge text → `{t('status.ok')}`

- [ ] **Step 5: Update `src/components/sheets/NewDestinationSheet.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`. The sheet renders inside a `<BottomSheet>` with a `title` prop — update that prop and all internal strings.

Replace:
- `title="New Destination"` on `<BottomSheet>` → `title={t('destination.newTitle')}`
- `"Auto-assigned icon"` → `{t('destination.autoIcon')}`
- `placeholder="e.g. Japan 2026"` → `placeholder={t('destination.titlePlaceholder')}`
- `aria-label="Trip title"` → `aria-label={t('destination.titleLabel')}`
- `"Title is required"` → `{t('destination.titleRequired')}`
- `"Start Date"` label → `{t('destination.startDate')}`
- `"End Date"` label → `{t('destination.endDate')}`
- `"End date must be after start date"` → `{t('destination.endDateError')}`
- `"Create Destination"` button → `{t('destination.create')}`

- [ ] **Step 6: Run all tests**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass.

- [ ] **Step 7: Build check**

```bash
pnpm build 2>&1
```

Expected: No errors.

- [ ] **Step 8: Commit**

```bash
git add src/pages/TripsPage.tsx src/pages/TripDetailPage.tsx src/components/DestinationRow.tsx src/components/sheets/NewDestinationSheet.tsx
git commit -m "feat: translate TripsPage, TripDetailPage, DestinationRow, NewDestinationSheet"
```

---

## Chunk 5: Event sheet, timeline, plans modal, local import

### File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/components/sheets/AddEventSheet.tsx` | Modify | Use `t()` for all strings |
| `src/components/TimelineEvent.tsx` | Modify | Use `t()` for type labels and aria-label |
| `src/components/PlansModal.tsx` | Modify | Use `t()` for all strings (source is hardcoded Portuguese) |
| `src/components/LocalDataImport.tsx` | Modify | Use `t()` for banner and buttons |

---

### Task 8: Translate AddEventSheet and TimelineEvent

- [ ] **Step 1: Read the two files in full**

Read:
- `src/components/sheets/AddEventSheet.tsx`
- `src/components/TimelineEvent.tsx`

- [ ] **Step 2: Update `src/components/sheets/AddEventSheet.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

The sheet title is passed to `<BottomSheet title={...}>`:
- `isEdit ? 'Edit Event' : 'Add Event'` → `isEdit ? t('event.editTitle') : t('event.addTitle')`

The type pill labels in `EVENT_TYPES` array — replace the `label` strings:
- `'Transport'` → `t('eventTypes.transport')`
- `'Stay'` → `t('eventTypes.accommodation')`
- `'Ticket'` → `t('eventTypes.ticket')`
- `'Food'` → `t('eventTypes.restaurant')`

> Note: `EVENT_TYPES` is a module-level constant. Move it inside the component function body (after `const { t } = useTranslation()`) so it can use `t()`. **Important:** The existing JSX renders it as `EVENT_TYPES.map(t => ...)` — the loop variable `t` will shadow the translation `t` from `useTranslation()`. Rename the loop variable to `typeItem` everywhere in that `.map()` call.

The type placeholders in `TYPE_PLACEHOLDERS` record — same approach: move inside the component and use `t()`:
- `transport` → `t('event.placeholderTransport')`
- `accommodation` → `t('event.placeholderAccommodation')`
- `ticket` → `t('event.placeholderTicket')`
- `restaurant` → `t('event.placeholderRestaurant')`

For transport type, there are separate `from` / `to` place fields. Replace their placeholders:
- `"🛫 From: departure place"` → `t('event.placeholderFrom')`
- `"🛬 To: arrival place"` → `t('event.placeholderTo')`

For non-transport, the place field uses `"📍 Search place"` → `t('event.placeholderPlace')`.

Replace all other strings:
- `aria-label="Event title"` → `aria-label={t('event.titleLabel')}`
- `"Title is required"` → `{t('event.titleRequired')}`
- `"Place is required"` → `{t('event.placeRequired')}`
- `aria-label="Event date"` → `aria-label={t('event.dateLabel')}`
- `"Date is required"` → `{t('event.dateRequired')}`
- `aria-label` for time: transport → `t('event.departureTimeLabel')`, other → `t('event.timeLabel')`
- `"Arrival time (optional)"` placeholder → `t('event.arrivalTimePlaceholder')`
- `"Time is required"` → `{t('event.timeRequired')}`
- `"Arrived on foot"` checkbox label → `{t('event.arrivedOnFoot')}`
- `"Cost (optional)"` placeholder → `t('event.costPlaceholder')`
- `aria-label="Cost"` → `aria-label={t('event.costLabel')}`
- `"Must be a positive number"` → `{t('event.costError')}`
- `"Any notes..."` placeholder → `t('event.notesPlaceholder')`
- `aria-label="Notes"` → `aria-label={t('event.notesLabel')}`
- Submit button: `isEdit ? 'Save Changes' : 'Add to Timeline'` → `isEdit ? t('event.saveButton') : t('event.addButton')`
- `"Delete event"` button → `{t('event.deleteButton')}`
- Delete confirmation label (add above the confirm/cancel buttons if not present): `<p>{t('event.deleteConfirmLabel')}</p>`
- `"Confirm delete"` button → `{t('event.confirmDeleteButton')}`
- `"Cancel"` button → `{t('event.cancelButton')}`
- `window.confirm('Delete this event?')` — if still present, replace with `window.confirm(t('event.deleteConfirm'))`. If there is no `window.confirm` (inline confirmation UI instead), skip this.

- [ ] **Step 3: Update `src/components/TimelineEvent.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

> **Note on existing tests:** If any existing `TimelineEvent` tests assert visible text `'Accommodation'` or `'Restaurant'`, update them to expect `'eventTypes.accommodation'` and `'eventTypes.restaurant'` (the mock returns the key string).

The `TYPE_LABELS` constant maps `EventType` to display strings. Replace it with `t()` calls inline, or make it a function that takes `t`:

```tsx
const TYPE_LABELS: Record<EventType, string> = {
  transport: t('eventTypes.transport'),
  accommodation: t('eventTypes.accommodation'),
  ticket: t('eventTypes.ticket'),
  restaurant: t('eventTypes.restaurant'),
}
```

> Move `TYPE_LABELS` inside the component function body (after `const { t }`) so it can use `t()`.

Replace the aria-label:
- `aria-label={\`Edit ${event.title}\`}` → `aria-label={t('event.editLabel', { title: event.title })}`

- [ ] **Step 4: Run all tests**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass.

- [ ] **Step 5: Build check**

```bash
pnpm build 2>&1
```

Expected: No errors.

- [ ] **Step 6: Commit**

```bash
git add src/components/sheets/AddEventSheet.tsx src/components/TimelineEvent.tsx
git commit -m "feat: translate AddEventSheet and TimelineEvent"
```

---

### Task 9: Translate PlansModal and LocalDataImport

- [ ] **Step 1: Read `src/components/PlansModal.tsx` and `src/components/LocalDataImport.tsx` in full**

Read both files.

- [ ] **Step 2: Update `src/components/PlansModal.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Replace the `ANNUAL_EQUIV` constant with `t()` calls inline (inside the component):
- Remove the `const ANNUAL_EQUIV = {...}` module-level constant entirely
- Where `ANNUAL_EQUIV.premium` is used → `t('plans.annualEquivPremium')`
- Where `ANNUAL_EQUIV.pro` is used → `t('plans.annualEquivPro')`

The `PRICES` constant stays (it holds R$ values that are fixed regardless of language).

Replace all remaining strings:
- `aria-label="Close"` → `aria-label={t('plans.close')}`
- `"Choose your plan"` → `{t('plans.heading')}`
- `"Upgrade anytime. Cancel anytime."` → `{t('plans.subtitle')}`
- `"Monthly"` button → `{t('plans.monthly')}`
- `"Annual"` button → `{t('plans.annual')}`
- `"−30%"` span → `{t('plans.savingsBadge')}`
- `"MOST POPULAR"` → `{t('plans.mostPopular')}`
- `"FREE"` badge → `{t('plans.free1trip').replace('✓ ', '').replace('1 ', '').toUpperCase()}` — **no**, just use `"FREE"` / `"PREMIUM"` / `"PRO ✦"` as-is; these are brand tier names, not translated. Only translate the feature bullets and UI chrome.
- `"sempre grátis"` → `{t('plans.alwaysFree')}`
- `/ano` and `/mês` span text → `{t('plans.perYear')}` and `{t('plans.perMonth')}`
- `"= R$13,25/mês · economize R$69"` → `{t('plans.annualEquivPremium')}`
- `"= R$27,40/mês · economize R$139"` → `{t('plans.annualEquivPro')}`
- Feature bullet `"✓ 1 viagem"` → `{t('plans.free1trip')}`
- Feature bullet `"✓ Até 10 eventos por viagem"` → `{t('plans.free10events')}`
- Feature bullet `"✓ Até 10 viagens"` → `{t('plans.premium10trips')}`
- Feature bullet `"✓ Até 30 eventos por viagem"` → `{t('plans.premium30events')}`
- Feature bullet `"✓ Compartilhar viagens"` (both Premium and Pro) → `{t('plans.shareTrips')}`
- Feature bullet `"✓ Até 30 viagens"` → `{t('plans.pro30trips')}`
- Feature bullet `"✓ Eventos ilimitados"` → `{t('plans.proUnlimited')}`
- `"Current plan"` disabled button text → `{t('plans.currentPlan')}`
- `"Get Premium"` → `{t('plans.getPremium')}`
- `"Get Pro"` → `{t('plans.getPro')}`
- `"Pagamento seguro via Stripe · Cancele quando quiser"` footer → `{t('plans.footer')}`

- [ ] **Step 3: Update `src/components/LocalDataImport.tsx`**

Add `const { t } = useTranslation()` from `react-i18next`.

Read the file first (Step 1) to know exact current strings, then replace:
- Banner plural text using `t('localImport.banner', { count: tripCount })` (i18next selects `_one` or `_other`)
- Subtitle text → `t('localImport.subtitle')`
- Importing state text → `t('localImport.importing')`
- Import button → `t('localImport.importButton')`
- Dismiss button → `t('localImport.dismiss')`

- [ ] **Step 4: Run all tests**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass.

- [ ] **Step 5: Build check**

```bash
pnpm build 2>&1
```

Expected: No errors.

- [ ] **Step 6: Commit**

```bash
git add src/components/PlansModal.tsx src/components/LocalDataImport.tsx
git commit -m "feat: translate PlansModal and LocalDataImport"
```

---

## Chunk 6: Final verification

### Task 10: Full test suite + build + manual smoke test

- [ ] **Step 1: Run full test suite**

```bash
pnpm exec vitest run 2>&1
```

Expected: All tests pass.

- [ ] **Step 2: TypeScript build**

```bash
pnpm build 2>&1
```

Expected: No errors, no warnings.

- [ ] **Step 3: Manual smoke test**

```bash
pnpm dev
```

Open http://localhost:5173 and verify:
1. App loads in Portuguese (or browser language if set to en/es)
2. On ProfilePage, a "Language / Idioma" row shows PT · EN · ES buttons
3. Clicking EN → all visible strings switch to English immediately (no reload)
4. Clicking ES → all visible strings switch to Spanish
5. Clicking PT → returns to Portuguese
6. Refresh page → selected language persists (localStorage `i18nextLng`)
7. Open the Plans modal → cards show translated strings
8. Add/edit an event sheet → all labels, placeholders, and errors are in the active language
9. Gap warning card shows translated text when two accommodations have no transport
10. Log out → language preference persists for next login (it's in localStorage, not Supabase)
11. Bottom nav tabs show `"Trips"` / `"Viagens"` / `"Viajes"` and `"Profile"` / `"Perfil"` correctly per active language
12. (Optional) Test `LocalDataImport` banner: open DevTools → Application → localStorage, set `tripmate-trips` to `{"state":{"destinations":[{"id":"x","title":"Test","emoji":"✈️","startDate":"2026-01-01","endDate":"2026-01-07","events":[],"createdAt":""}]},"version":0}`, reload → banner appears in the active language

Stop server with Ctrl+C.

- [ ] **Step 4: Final commit if any minor fixes made during smoke test**

```bash
git add -p
git commit -m "fix: i18n smoke test fixes"
```

---

## Summary

When complete:
- `pnpm exec vitest run` → All tests pass
- `pnpm build` → No TypeScript errors
- ProfilePage has a Language row with PT · EN · ES buttons
- Language persists in `localStorage` key `i18nextLng`
- Browser language auto-detected on first visit; `"pt"` and unknowns fall back to `pt-BR`
- Every user-visible string in all pages and components is translated
